// The ESM authoring shape (a default export), over the same table as
// team.pack.cjs: `packs build` must compile both to the same committed JSON.
import pack from "./team.pack.cjs";

export default pack;
